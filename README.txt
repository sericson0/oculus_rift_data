The code scrapes oculus rift game metadata
-Run Get_Url_List.py first
-Run Get_Game_Data.py second
-Run Compile_CSV_Files.R last (Note this is an R script to compile the csv results into a single csv

Requirements
-selenium and pandas
-Chrome browser the same as installed chromedriver
-R code should be run from R studio (Otherwise path needs to be changed)

Please let me know if you have any issues
